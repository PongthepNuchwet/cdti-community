pip install -r requirements.txt
pip freeze > requirements.txt 
heroku config:add TZ="Asia/Bangkok"

import datetime
import pytz

utc_now = pytz.utc.localize(datetime.datetime.utcnow()).astimezone(pytz.timezone("Asia/Bangkok"))